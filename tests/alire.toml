name = "machine_types_tests"
description = "Tests for the built-in Alire test runner"
version = "0.0.0-test"

[[depends-on]]
machine_types = "*"

[[pins]]
machine_types = { path = ".." }

[build-profiles]
machine_types       = "validation"
machine_types_tests = "validation"
